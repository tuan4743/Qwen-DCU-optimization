===== image 5.png =====
/ * 溽 码 (baseline) #if deftned( __gfx9Øa #deftne HIP GFX9 #endtf #if deftned( __gfx9Øa #deftne HIP GFX9 #endtf #if deftned( __gfx936 目 deftned( __gfx942 目 def tned(__gfx950 目 def tned(__gfx936_ ） / ． 入 [ 还 凵 ， / ． 改 5 h f [ #deftne HIP_DCU_GFX93X #endtf LL 卜 櫚 1 可 过 * ／ 归 约 ， 不 司 靠 mov_dpp /

===== image 8.png =====
# 码 (baseltne) use_skinny and on_gfx9( ） # 仇 化 and (on_gfx9( ） 0 r on_gfx936( 月 and rocm_skinny_ops_available( ） USe if on_gfx936( ） and n 1 and m 4 @ and k < = 8192 and bias tS None: .LLMM1(weight ， x-view ， 4 ） # 瘦 GEMV; k > 8 四 2 数 值 不 可 靠 故 封 顶 out OPS

===== image 4.png =====
# 源 码 (baseltne) eltf is_cuda alike( ） and has_devtce_capabtIity(8@): return 128 # DCU 误 判 “ LDS thrash # 仇 化 if is rocm( ） and head_s tze tS not None and head_size > = return 32 # 控 shared ， 压 Pref TTFT 255 ：

===== image 19.png =====
# 码 (baseline) NUM_PAR_SOFTMAX_SEGMENTS # 伉 化 NUM_PAR_SOFTMAX_SEGMENTS 16 = 32 # 提 升 长 上 下 文 Decode 并 行 度

===== image 9.png =====
# 泪 蚂 (baseline) # 保 复 、 减 HBM tI. load( K_ptrs ， tI. load(K_ptrs, tI. load( V_pt rs ， mask= mask= mask= other other=O other=@ eviction_policß'evict_last 衲 ） eviction_poIiCF"evict_Iast ）

===== image 7.png =====
# 源 犸 (basel NUM_PAR_SOFTMAX_SEGMENTS = 16 # 仇 化 NUM_PAR_SOFTMAX_SEGMENTS = 32 # 长 、 上 下 文 多 下 行 、 上 限 def _flash_decode_segment 5 （ max_seq_len ， max_segments): if max_seq_len < = 2048 ： ret u rn mtn( 16 ， max_segments ） ideal (max_seq_len 十 255 ） / / 256 1 < < max （ ideal 1 ， O).btt_Iength( ） SegS return max( 16 ， min （ max_segments ， segs)) # ～ 256 tOk/ 段 # 2 次 幂

===== image 20.png =====
# 泪 蚂 (baseline) BLOCK = get_bloc k_stze （ dtype ） # 易 误 选 128 def get_preftll_ttles( dtype ， head_size ， max_input_len): if is_rocm( ） and head_size > = 255 ： if max_tnput_len > = 81g2 ： # Bm,Bn,warps ， stages: 非 对 称 十 深 流 水 撼 HBM return 32 ， 16 ， 8 ， 3 return 32 ， 32 ， 8 ， 2 # Unified: PrefiLl TILE=16; 长 KV warps=8/5tages=3

===== image 16.png =====
# 源 码 〔 baseltne) BLOCK 凹 = 16 tf num_quertes_per_kv < = 16 else trtton ． next_power_of_2( num_queries_per_kv) # 优 化 tf ts rocm( ） and head_stze > = 256 and max_seqlen_q > 1 and num_quertes_per_kv > 1 ： BLOCK_M = trtton. next-power-of-2( max( BLOCK-M ， 32 ） ） # 满 足 2 次 幂 约 束 并 摊 销 长 KV BLOCK_M / / num_queries_per_kv # 丬 BLOCK_Q

===== image 10.png =====
# 源 码 (baseltne) BKV_ LI ST = [ 64 ， 128 # stages [ 2 ， 3 4 ] tf check_shared_mem( ） else 32 64 ] # BKV=128 易 00M # 优 化 tf ts rocm( ） ： BKV_LIST = [ 6 NUM WARPS NUM STAG ES = # FLA GDN FIX BT - BT=6 # 禁 BKV=128 # 去 掉 stages=4

===== image 18.png =====
# 源 码 (baseltne) BLOCK M = 16 tf GQA < = 16 else next_power of 一 2 （ GQA) # 小 瓦 片 ， 循 环 多 TILE SIZE PREFILL = _get_ttle stze( · # 优 化 use_large_qwen_preftll_ttle ts_rocm( ） and head_stze 256 and GQA 6 and max_seqlen k > = 4@96 # 仅 长 Preftll and max_seqlen_q > 1 BLOCK_M = next_power_of_2(max( BLOCK 凹 ， 128 tf use_large_qwen_preftll_tlle: TILE SIZE PREFILL = 32 num_warps ， num stages = 8 1 # stages=l 控 大 卜 1 × N 的 tf use_large_qwen_preftll_ttle else 32 LDS/VGPR

===== image 21.png =====
# 源 码 （ b a s e I n e 〕 BV = min( next_power of 一 2 （ V ） ， 1 num_warps # 优 化 32 ） ts_qwen35_decode = HV = 48 and V BV = 128 tf ts_qwen35_decode else num_warps = 4 tf is_qwen35_decode # “ NV=4 128 and K 128 mtn( next_power_of_2(V) ， else 1 # 掩 盖 H BM 32 ）

===== image 12.png =====
# 源 码 〔 ba s e I t n e ） qkvz, ba self 。 tn_proj_qkvz( h ） self ． tn_proj_ba(h) # 优 化 tf self._fused_in_proj_weight tS fused rocm_unquanttzed_gemm(), self qkvz ， ba spltt(fused) OS · envt ron [ "TORCH_BLAS_PREFER_HIPBLASLT" ] and n u m tokens 1 ． # 单 次 GEMV fused_tn_proj_wetght None) # M=I 避 开 htpBLASLt 劣 化

===== image 30.png =====
# 优 化 前 TILE SIZE PREFILL = 32 # 优 化 ttle n tnt(os.getenv( "VLLM_ROCM_FA PREFILL TILE" TILE SIZE PREFILL ttle n tf ttle n tn （ 16 ， 32 ， 64 ） = 8 ， 1 # 大 N 保 持 stages=l ， 控 num_warps, num stages " 64 " ） ） # 默 认 64 ： 可 选 16 / 32 else 64 LDS

===== image.png =====
# 源 码 〔 ba s e I t n e ） —proj_wetght qkvz, # 优 化 self.tn —"OJ_qkvz(h); ba ， self · tn_proj_ba(h) tf self. fused tn tS fused rocm_unquanttzed_gemm(h ， self fused[ ． qkvz, ba tf n u m tokens 1 :spltt], fused[ ． # Preftll + Decode fused_tn_proj_wetght None) split: ] qkvz ， ba qkvz.conttguous( ), # P 「 ef t I I ： 直 接 用 vtew, 避 免 Txout # Decode 保 s t r t d e ba. conttguous( ） 《 次 HBM 与

===== image 1.png =====
# 源 码 〔 ba s e I t n e ） ： 多 kernel + 中 间 A 写 HBM # 优 化 ： 模 块 入 库 ： 默 认 关 闭 ， 按 高 「 ] 控 开 启 0 s · envtron · setdefault( "VLLM_ROCM_GDN_FUSED_PREPROC "


