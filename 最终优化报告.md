# 最终优化报告:Qwen3.5-27B · 海光 DCU(gfx936)· vLLM 长上下文推理优化

> 模型:Qwen3.5-27B;框架:vLLM 0.18.1+das.dtk2604;硬件:海光 DCU gfx936(BW3000,80 CU,64GB HBM)
> 详细证据/校验见 `docs/` 与 `优化记录/`。

---

## 1. 任务与硬约束

- 目标:单请求(并发=1)在线推理,评测三段输入长度 **4-8K / 8-16K / 16-32K**,指标 = Output Token Throughput(越高越好)、P99 TTFT、P99 TPOT。
- 可动:KV Cache/显存管理、Decode 算子、执行路径/调度、算子级非持久化低精度、改 vLLM 框架代码、安装依赖/编译 custom kernel。
- 不可动:模型权重与结构、模型/tokenizer/scheduler/sampling 等锁定参数、投机解码、评测作弊;官方目标吞吐 21.4/19.81/16.32 tok/s。

## 2. 基线(2026-07-14 评测机实测)

| 段 | Output 吞吐 (tok/s) | P99 TTFT (ms) | P99 TPOT (ms) |
|---|---|---|---|
| 4-8K | 12.20 | 4789.70 | 69.00 |
| 8-16K | 8.81 | 25046.71 | 70.48 |
| 16-32K | 4.64 | 28744.39 | 71.89 |

瓶颈画像(profile + 归因,详见 `优化记录/03_profile_findings.md`、`10/12/15`):
- 256MB fill = Triton autotune L2-flush buffer(非 vLLM 业务 buffer),消除后吞吐 7.26 < 8.8 → 证伪。
- GPU duty 97.3%,idle 全 <1ms → 瓶颈在 **step 内部 kernel 串行**,不在 CPU/调度、不在 step 之间。
- decode 时间主体 = GEMM(`MT64x32x32_GSU1` 等 rocBLAS tile,gate_up 为最大头,占 GPU 时间 48%),GDN 递归核仅 0.9%。
- 16-32K 崩塌主因 = cudagraph ON 下 full-attn 走 3D flash-decoding,段数固定 32,长 KV reduce tax 非线性急升;Prefill 是独立路径(TTFT 28.7s)。

## 3. 优化总览:五轮

| 轮 | 主题 | 4-8K | 8-16K | 16-32K | P99 TTFT(4-8K) | P99 TPOT(4-8K) |
|---|---|---|---|---|---|---|
| 基线 | — | 12.20 | 8.81 | 4.64 | 4789.70 | 69.00 |
| 一 | DCU 瘦 GEMV(LLMM1)+ Prefill 瓦片校准 | 16.20 | 9.44 | 3.64* | 10454.95* | 47.01 |
| 二 | 长上下文:自适应 Flash-Decoding + 非对称 Prefill 瓦片 | 16.05 | 9.61 | 4.39 | 12706.21 | 46.99 |
| 三 | GDN/FLA 定参 + BLOCK_M 二次幂 | 17.25 | 12.20 | 6.99 | 9053.87 | 47.53 |
| 四 | 大瓦片 Prefill FA + Decode GDN 融合 | 19.12 | 14.90 | 9.73 | 1933.92 | 47.09 |
| 五 | TILE_N 扩维 + 跨阶段 in_proj 融合 + 融合 Chunk 预处理 | **19.56** | **14.92** | **12.22** | **1964.41** | **45.14** |

\* 一、二轮 16-32K/4-8K 的 TTFT 数字低于基线且吞吐不单调,疑为分轮独立测量;以终稿汇总表为准(见 `docs/00` §4)。

**总效果:4-8K 1.60×、8-16K 1.69×、16-32K 2.63×;P99 TPOT −34.6%;P99 TTFT −59%。**

### 3.1 第一轮:DCU 瘦 GEMV(LLMM1)与 Prefill 瓦片校准
- **1.1 瘦 GEMV 路由**:(a) C++ 侧 `skinny_gemms.cu` 把 `__HIP__GFX9__` 宏条件纳入 `gfx936/938`(否则 DCU 构建下 wvSplitK 是 `assert(false)` 空壳),新增 `__HIP_DCU_GFX93X__`(shfl 归约、禁不可靠 mov_dpp);(b) Python 侧 `rocm.py` 新增 `on_gfx936()` 谓词,`utils.py` 的 `use_skinny` 打开,且 **仅 n==1、m%4==0、k≤8192、无 bias 时路由到 `ops.LLMM1`(128bit 合并访存)**,k>8192 硬封顶回退通用路径避免数值漂移;`TORCH_BLAS_PREFER_HIPBLASLT=0` 保证 M=1 不落入 hipBLASLt。**这是把 decode 投影从 500us 级(rocBLAS/hipBLASLt)降到 ~127-188us 的关键。**
- **1.2 Prefill BLOCK=32**:DCU 上报 capability 9.x,源码 `has_device_capability(80)` 误选 BLOCK=128,Qwen hd=256 下 Q/K/V 片上工作集膨胀 → LDS thrash / 64KB 级失败。ROCm 且 hd≥256 强制 32。
- **1.3 Flash-Decoding 分段 16→32**:B≈1 长 decode 各 segment 并行度不足,分段×2(online-softmax 数学等价)。
- **1.4 KV 访存 evict_last**:unified attention K/V `tl.load` 加 `eviction_policy="evict_last"`,长序列扫 KV 减 cache 冲刷。

### 3.2 第二轮:长上下文 Prefill/Decode 调优
- **2.1 自适应 Flash-Decoding**:`NUM_PAR_SOFTMAX_SEGMENTS=32` 为上限,新增 `_flash_decode_segments`(≤2048→min(16,ms);否则 ~256 tok/段取 2 次幂,**eager 按 seq 自适应, CUDA Graph 捕获时固定 grid Z**)——化解了"自适应 vs cudagraph 固定 grid"冲突,避免长段 reduce tax。
- **2.2 非对称 Prefill 瓦片 + 软件流水**:`max_input_len≥8192` 时 `(BLOCK_M,BLOCK_N)=(32,16)`、`num_stages=3`(多缓冲掩盖 HBM 往返),短输入 (32,32)/stages=2;Unified Attention Prefill 配套 TILE=16 防 shared OOM。

### 3.3 第三轮:GDN/FLA 定参与 BLOCK_M 二次幂
- **3.1 BLOCK_M 二次幂对齐**:ROCm 且 hd≥256、长序列:`BLOCK_M=next_power_of_2(max(Bm,32))`(Triton 对 `tl.arange(0,BLOCK_M)` 有 2 次幂要求);Qwen GQA=6 → Bm=32、BQ=5。
- **3.2 FLA Chunk 定参**:ROCm 钉 `BKV_LIST=[64]`(禁 128 防 OOM)、`NUM_WARPS=[4,8]`、`[2,3]` 两档 stages(去 4)、`BT=64`(FLA_GDN_FIX_BT),把 autotune 从"大而易 LDS 重配"收窄为"稳而准"。

### 3.4 第四轮:大瓦片 Prefill FA 与 Decode GDN 融合 ★
- **4.1 Prefill 大瓦片 FA(M=128,N=32)**:仅长 Prefill 命中(`q>1 && k≥4096` 门控):BLOCK_M≥128、TILE 32、**num_stages=1**(大 M×N×hd 工作集禁深流水,防 LDS/VGPR 超限);减 program 数与 launch 税、减 KV 内层循环。
- **4.2 GDN Decode 值维瓦片 BV=128**:Qwen3.5 形状(HV=48,V=K=128)特化:单 program 覆盖整段 V(原 BV=min(pow2,32)→NV=4 次重载),`num_warps=4` 提 wavefront 掩盖 HBM。
- **4.3 Decode in_proj 融合 + hipBLASLt 旁路**:每 GDN 层原 qkvz+ba 两次 Linear → **权重行维拼接一次 GEMV**;强制 HIPBLASLT=0 保 TPOT。

### 3.5 第五轮:FA TILE_N 扩维与跨阶段 in_proj 融合 ★
- **5.1 TILE_N 32→64**:`VLLM_ROCM_FA_PREFILL_TILE`(默认 64,可选 16/32/64),大瓦片下 KV 内层循环约减半;保持 stages=1。
- **5.2 Prefill/Decode 融合 in_proj**:`load_weights` 后拼接 `_fused_in_proj_weight`(不落盘、不改数值),`Y=X[Wqkvz;Wba]` 数学等价——**每 GDN 层每个 chunk 少一次权重 HBM 遍历与一次 kernel launch**;Prefill 对 view 不强制 `.contiguous()`(避免 T×out 二次写回),Decode 保 contiguous 稳 stride。
- **5.3 融合 Chunk 预处理**:cumsum⊕KKT⊕tril⊕WU 融核入库,`VLLM_ROCM_GDN_FUSED_PREPROC` 默认 0(按需门控;默认关闭故对默认跑分无贡献,是备用优化)。

### 3.6 为什么"融合"是全场最有价值的部分
第四轮单轮:4-8K 吞吐 +1.87、16-32K +2.74、TTFT 9.05s→1.93s(−79%);第五轮把 16-32K 推到 2.6× 基线,TPOT 降至全场最低 45.14ms。全部分布于 **Python 模型层 + vendored Triton/FLA**:不需要重编 `_rocm_C.so`,与"手写 split-K GEMM 算子(需 4-5 对接点+wheel 编译,最终未能集成)"是完全独立的两条通道——**算子开发失败 ≠ 融合无效**。19 号总结文档(07-15)中"融合=死路、唯一收益=rocm.py 一处"系过早结论,已于 `docs/02_summary_corrections.md` 勘误。

## 4. 关键经验(浓缩)

1. **归属必须先 profiler 实测核对**:GEMM 后端归属不能用源码静态推断(源码版 ≠ 线上 dist-packages 版);`GSU` 正则同名撞车曾把 rocBLAS tile 误吞成"GDN 95.17%",订正后真实 FFN_GEMM 占 95.55%。
2. **duty cycle 97.3%** 推翻"step 间空闲";`median_gap=1ms` 是层间 kernel 间隔而非跨 token 空闲。
3. **cudagraph ON 下 op 标签归因不可行**(record_function 不进静态图)→ 用 shape→tile 正向匹配 + 频次/邻接反推闭合归属。
4. **空壳陷阱**:`csrc` 宏名单不含 gfx936 → 空载 kernel `assert(false)`;任何"Python 侧开链、C++ 侧没宏"的配对都要先查编译宏。**终稿 1.1 的 HIP 宏修正就是这个坑的解法。**
5. **融合的正确姿势**:优先数学等价型(权重拼接、分块参数、门控调度),避免跨算子单 kernel(收益上限 ~5%)。
6. **验证铁律**:warmup+1000 次 median、bf16 容差、trace 核名校验、装 wheel 后校验 site-packages、`ast.parse` 语法即时校验。

## 5. 还原可信度与风险(必读)

| 项 | 说明 |
|---|---|
| 数字来源 | 五轮数字与截图 = 比赛终稿说明文档(容器 07-15 09:04 截图);内部记录(07-13 止)只有 C1(+42.6%)等早期数据,二者口径差异已在 `docs/00 §4` 列出(如官方给定 16-32K baseline=5.38 vs 终稿 4.64;一轮 16-32K 3.64<基线) |
| 最终代码 | `最终修改版/` = 按截图重建(标注置信度);**未在机器上验证**。恢复可用:`git diff --no-index 修改前原版/<f> 最终修改版/<f>` |
| 未重建项 | `最终修改版/README_变更清单.md` 列出(attention.cu、reshape_and_cache_flash、backends/utils、chunk_delta_h 定参、causal_conv1d、version.py 等) |
| 截图像素级存疑 | 视觉转录+OCR 双源,仍有个别字符存疑(宏名单第 4 个 arch、`is_roc m()`、`fused_in_proj_weight` vs `_fused_in_proj_weight`),以原始截图为准 |
| 最大风险 | 1.1a 终稿路由没有 m 上限 → lm_head(m=248320)也进 LLMM1;**内部 C4-3 实验曾证明"lm_head 动 wvSplitK 会灾难",恢复期必须 A/B 该点**,必要时补 m 上限 |

## 6. 附录

- **项目结构**:`README.md`(本索引)、`最终优化报告.md`(本文)、`最终修改版/`(12 文件+变更清单)、`修改前原版/`(同 12 文件基线)、`优化记录/`(任务文档 01-19 + 终稿说明文档 + 40 截图 + 工具脚本 + log)、`profile/`(pmc/trace/解析结果)、`docs/`(00 校验报告、01 规格表、02 勘误、03 资产地图、04 OCR 底稿)。
- **原始工作区**:`vllm_optimize_data/`(完整数据,vllm_cscc 源码树、tasks、tools、profile 等,未移动)。
- 机器/容器(`/public/home/xdzs2026_c150/zya`、评测容器)已收回,不可再验证任何条目。
